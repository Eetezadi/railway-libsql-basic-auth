# libSQL Database Configuration
# Copy this file to .env and fill in your credentials

# Database URL (format: https://your-database.railway.app or libsql://your-database.turso.io)
# Do NOT include username:password in the URL - they are specified separately below
DB_URL=https://your-project.up.railway.app

# HTTP Basic Auth Credentials
# Username for database access (default is usually "root")
DB_USER=root

# Password for database access (use a long, random string)
DB_PASSWORD=your-password-here
